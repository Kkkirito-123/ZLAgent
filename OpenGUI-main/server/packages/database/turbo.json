{
	"$schema": "https://turborepo.com/schema.json",
	"extends": ["//"],
	"tasks": {
		"build": {
			"dependsOn": ["db:generate"],
			"outputs": ["dist/**"]
		},
		"db:generate": {
			"outputs": ["generated/**"]
		}
	}
}
